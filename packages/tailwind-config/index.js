/**
 * Shared Tailwind v4 design tokens for SalesAI web apps.
 * Tailwind v4 is configured via CSS (@theme); this file exports tokens that
 * apps can import for JS-side usage (charts, canvas avatar, etc).
 */
export const tokens = {
    colors: {
        brand: '#6d5efc',
        brandDark: '#4b3fd6',
        bg: '#0b0b12',
        surface: '#15151f',
        text: '#f5f5fa'
    },
    radius: '12px'
};
